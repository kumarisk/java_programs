import static number.Primeno.*;
import static number.maxno.*;
import static number.armstrong.*;
class NumberEx
{
	public static void main(String args[])
	{
		int a=max(30,20);
		Prime(5);
		armstrong(153);
		System.out.println("\nMax no:"+a);
	}
}
