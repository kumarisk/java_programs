import number.Square;
class SquareEx
{
   public static void main(String args[])
   {
      Square s=new Square(6);
	  System.out.println("\nResult="+s.result());
   }
}
//(Note: save the above file using SquareEx.java into the curerent directory of number package.)